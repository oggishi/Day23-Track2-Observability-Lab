.env
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.pytest_cache/

# Lab artifacts (generated)
benchmarks/*.csv
benchmarks/*.json

# Local OS junk
.DS_Store
Thumbs.db

# Drift: ignore heavy/regenerable inputs + the ~3MB Evidently HTML report
# (rubric #17 is graded by screenshot), but KEEP the small summary that
# `make verify` and rubric #16 read from a clean clone.
04-drift-detection/data/
04-drift-detection/reports/*.html
04-drift-detection/drift-summary.json

# AgentOps bonus: ignore the scratch copies the runner drops at the repo root
# and in BONUS-agentops/, but KEEP the committed submission deliverable (B3).
/agentops-report.json
BONUS-agentops/agentops-report.json
